# Optional custom fonts

Hero text uses **Pacifico** (Google Fonts) with ♥ symbols on the dots of **i** and **j** in English titles.

To use your own font file instead, add it here and wire it up in `src/app/layout.tsx`.
